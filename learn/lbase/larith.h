#ifndef LARITH_H
#define LARITH_H

extern int larith_max(int x, int y);
extern int larith_min(int x, int y);
extern int larith_div(int x, int y);
extern int larith_mod(int x, int y);
extern int larith_ceiling(int x, int y);
extern int larith_floor(int x, int y);

#endif
