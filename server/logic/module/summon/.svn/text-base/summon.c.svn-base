#include <summon_key.h>
#include <macros.h>

mapping SaveMap()
{
	return ([
			/* 存盘数据 */
			// 召唤兽类型
			K_SUMMON_TYPE : 0, 
			// 召唤兽等级
			K_SUMMON_GRADE : 0,
			// 经验
			K_SUMMON_EXP: 0,

			K_SUMMON_NAME:"",
			K_GLOBAL_KEY:"",
			K_SUMMON_EQUIP_BAGID : 0,
		]);
}

void InitSummon(int summonid)
{
	// debug_message( sprintf("new summon %x, %d", summonid, summonid) );
}
