#ifndef SWAP_H
	#define SWAP_H

	/*
	 * swap.c
	 */
	int swap(object_t*);
	int swap_line_numbers(program_t*);
	void load_ob_from_swap(object_t*);
	void load_line_numbers(program_t*);
	void remove_swap_file(object_t*);
	void unlink_swap_file(void);
	void remove_line_swap(program_t*);
	int locate_in(program_t*);
	int locate_out(program_t*);
	void print_swap_stats(outbuffer_t*);

#endif
